import { FirstDirective } from './first.directive';

describe('FirstDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstDirective();
    expect(directive).toBeTruthy();
  });
});
